# Production server inventory

These tab-separated files were exported from the SheepEpimap production
server on 2026-09-10. `environments_20260910.tsv` lists the conda environments;
`conda_packages_20260910.tsv` lists every package and its version without
machine-specific prefixes. Use the versioned workflow YAMLs in
`pipelines/Envs/` for Snakemake.
